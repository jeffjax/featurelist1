import featureDatasource from '../../../utils/feature-datasource';
import { module, test } from 'qunit';

module('Unit | Utility | feature datasource');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = featureDatasource();
  assert.ok(result);
});
